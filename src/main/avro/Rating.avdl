@namespace("com.redeyetechguy.kiji")

protocol NetflixRecords {

  /** Metadata for a rating. */
  record Rating {
    string user_id;
    int rating;
    string timestamp;
  }

  /** Array to store ids of recommended movies */
  record RecommendedMovies {
    array<string> movie_ids;
  }
}